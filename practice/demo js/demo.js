let welcomeBtn = document.querySelector('button');
welcomeBtn.addEventListener('click', showMsg);

function showMsg(){
    let name = prompt('Enter the name of the student');
    welcomeBtn.textContent;
}
